{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "git -C \"$CLAUDE_REPO_ROOT\" status --short | grep -q . && echo '⚠ Uncommitted changes — remember to commit before ending the session.' || true"
          }
        ]
      }
    ]
  }
}
