## Git discipline

After making any code change:
1. State which files were modified.
2. Ask the user: "Ready to commit? I'll suggest a message." before ending the task.
3. Never leave a session with uncommitted changes without explicitly flagging them.
4. Run `./scripts/check-uncommitted.sh` at the end of any multi-step task.
